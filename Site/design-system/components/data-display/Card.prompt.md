Generic surface container for dashboard panels, statement summaries, and content blocks.

```jsx
<Card><h3>Faturamento do mês</h3><p>R$ 48.200,00</p></Card>
```
Set `elevated={false}` for a flush block (e.g. nested inside another card).
