Underline tab bar with a gold active indicator — used to switch between statement periods, document types, settings sections.

```jsx
<Tabs items={[{value:'mes',label:'Este mês'},{value:'ano',label:'Este ano'}]} active={tab} onChange={setTab} />
```
