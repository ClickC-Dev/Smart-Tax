Custom checkbox for consent/agreement rows and multi-select lists (e.g. selecting invoices, documents).

```jsx
<Checkbox label="Recebi e conferi as notas fiscais" checked={done} onChange={e => setDone(e.target.checked)} />
```
